<Page x:Class="FPConnect.view.Pages.GestionUsuarios"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FPConnect.view.Pages"
      xmlns:fa="http://schemas.fontawesome.io/icons/"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="620" d:DesignWidth="800"
      Title="GestionUsuarios">

    <Grid Width="auto" Height="auto" Margin="30">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!--Imputs-->
            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 10">
                <materialDesign:PackIcon Kind="User" Width="25" Height="25" Margin="10" Foreground="Black"/>
                <TextBox x:Name="txtNombreUsuario" Margin="10 0" materialDesign:HintAssist.Hint="Introduce el nombre de usuario" Foreground="Black" Width="250" BorderBrush="Black" CaretBrush="#FFD94448" SelectionBrush="#FFD94448" />
            </StackPanel>

            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 10">
                <materialDesign:PackIcon Kind="Password" Width="25" Height="25" Margin="10" Foreground="Black"/>
                <PasswordBox x:Name="txtPassword" Margin="10 0" materialDesign:HintAssist.Hint="Introduce la contraseña" Foreground="Black" Width="250" BorderBrush="Black" CaretBrush="#FFD94448" SelectionBrush="#FFD94448" />
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 10">
                <materialDesign:PackIcon Kind="CardAccountDetails" Width="25" Height="25" Margin="10" Foreground="Black"/>
                <TextBox x:Name="txtNombre" Margin="10 0" materialDesign:HintAssist.Hint="Introduce el nombre" Foreground="Black" Width="250" BorderBrush="Black" CaretBrush="#FFD94448" SelectionBrush="#FFD94448" />
            </StackPanel>
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 10">
                <materialDesign:PackIcon Kind="CardAccountDetails" Width="25" Height="25" Margin="10" Foreground="Black"/>
                <TextBox x:Name="txtApellido" Margin="10 0" materialDesign:HintAssist.Hint="Introduce el apellido" Foreground="Black" Width="250" BorderBrush="Black" CaretBrush="#FFD94448" SelectionBrush="#FFD94448" />
            </StackPanel>
            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 10">
                <materialDesign:PackIcon Kind="Email" Width="25" Height="25" Margin="10" Foreground="Black"/>
                <TextBox x:Name="txtEmail" Margin="10 0" materialDesign:HintAssist.Hint="Introduce el email" Foreground="Black" Width="250" BorderBrush="Black" CaretBrush="#FFD94448" SelectionBrush="#FFD94448" />
            </StackPanel>
            <ComboBox x:Name="cbRol" materialDesign:HintAssist.Hint="Rol" FontSize="18" Grid.Row="5" Width="300" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 10">
                <ComboBoxItem Content="Profesor"/>
                <ComboBoxItem Content="Administrador"/>
            </ComboBox>

            <!--Botones-->
            <Grid Grid.Row="6" Margin="20 15 20 0" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>
                <Button x:Name="btnGuardar" Content="Guardar" FontSize="22" Width="auto" Height="40" Style="{StaticResource buttonMainGreenGestionUsuarios}" Grid.Column="0"/>
                <Button x:Name="btnCancelar" Content="Cancelar" FontSize="22" Width="auto" Height="40" Style="{StaticResource buttonMainGestionUsuarios}" Grid.Column="2"/>
            </Grid>
        </Grid>

        <Grid Grid.Column="1">

            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <DataGrid x:Name="dataGridEmpleados" Margin="10" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Id" Binding="{Binding idEmpleado}" Visibility="Hidden" Width="2"/>
                    <DataGridTextColumn Header="Nombre Empleado" Binding="{Binding nombre}" Width="150"/>
                    <DataGridTextColumn Header="Apellidos Empleado" Binding="{Binding apellidos}" Width="150"/>
                    <DataGridTextColumn Header="CSR Usuario" Binding="{Binding csr}"/>
                    <DataGridTextColumn Header="CSR Usuario" Binding="{Binding idUsuario}" Visibility="Hidden"/>
                    <DataGridTextColumn Header="ID Rol" Binding="{Binding idRol}"/>
                </DataGrid.Columns>
            </DataGrid>

            <Grid Grid.Row="1" Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>
                <Button Content="Modificar" Height="40" Style="{StaticResource buttonMainGreenGestionUsuarios}"/>
                <Button Content="Eliminar" Grid.Column="2" Height="40" Style="{StaticResource buttonMainGreenGestionUsuarios}"/>
            </Grid>

        </Grid>



    </Grid>

</Page>
